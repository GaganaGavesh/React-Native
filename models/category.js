class Category {
    constructor(id, title, color) {
        //this type of object property creation is belongs to vanila js
        this.id = id,
        this.title = title,
        this.color = color
    }
}

export default Category;